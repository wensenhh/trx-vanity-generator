## trx_vanity vX.Y.Z

Audience: CLI alpha users / developers
Commit: `<sha>`
Build matrix: `<platforms tested; explicitly list not-tested platforms>`
Signing status: unsigned
Notarization status: not applicable / not notarized
Checksums: `SHA256SUMS.txt` attached

## Artifacts

- `<filename>`: `<sha256>`

## Verification

```bash
shasum -a 256 -c SHA256SUMS.txt
```

## Known issues

- `<issue or limitation>`

## Security reminder

- Verify SHA256 before running.
- Do not share private keys, seed phrases, keystore files, full `results.csv`, `.env`, tokens, API keys, or encrypted export passwords.
- Do not upload complete result files to GitHub Issues, Telegram, cloud drives, or support channels.
- Vanity search is probabilistic; ETA is not a guarantee.
